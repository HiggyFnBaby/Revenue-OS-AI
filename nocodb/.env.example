# Copy this file to .env and fill in real values before running
# `docker compose up` in this directory.

# Password for the nocodb Postgres role. Generate with: openssl rand -base64 24
NC_DB_PASSWORD=

# Secret NocoDB uses to sign auth JWTs. Generate with: openssl rand -base64 32
NC_AUTH_JWT_SECRET=
